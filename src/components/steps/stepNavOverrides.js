// Navigation overrides for "branch" pages that should not follow ±1 linear nav.
// Populated by WhereToNow.jsx at module load time.
// Shape: Map<ComponentFn, { prevStep: ComponentFn, nextStep: ComponentFn }>
const stepNavOverrides = new Map();
export default stepNavOverrides;
