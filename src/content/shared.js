/**
 * Shared copy used across multiple pages.
 * Keep entries here only if they appear in more than one component.
 */
export const sharedContent = {
	showExample: { default: "Show example" },
	hideExample: { default: "Hide example" },
};
